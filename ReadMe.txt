To use the application create a realtime database with firebase.
Be sure to add Email and Password user authentication.
Add a appSecrets.js file to the util folder with the following defaultValues
        -- export const baseURLSecret = [You realtime database URL]
        -- export const authKeySecret = [You realtime database key]